"""Configuration module for the Tree Menu application."""
from django.apps import AppConfig


class TreeMenuConfig(AppConfig):
    """Configuration class for the Tree Menu application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "src.tree_menu"
